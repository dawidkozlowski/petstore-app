{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ action }} Pet</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">{{ action }}</button>
    {{ form_end(form) }}

    <a href="{{ path('home') }}" class="btn btn-secondary mt-3">Cancel</a>
{% endblock %}
